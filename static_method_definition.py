"""If you want to define a static method, you can use the @staticmethod decorator.
 A static method does not receive an implicit first argument (like self or cls).
   It behaves like a regular function but belongs to the class's namespace. @staticmethod can be called on the class itself or on an instance of the class."""